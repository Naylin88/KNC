@model IEnumerable<InitCMS.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h5>Category List</h5>
<hr />

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead style="background-color: gainsboro">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CatTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CatDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CatId"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a> 
                <a asp-action="Details" asp-route-id="@item.CatId"><i class="fa fa-info-circle" aria-hidden="true"></i></a> 
                <a asp-action="Delete" asp-route-id="@item.CatId"><i class="fa fa-trash" aria-hidden="true"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
